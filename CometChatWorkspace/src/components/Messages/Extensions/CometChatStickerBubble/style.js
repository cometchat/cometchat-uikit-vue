export const messageKitBlockStyle = props => {
	return {
		padding: "0",
		border: props.border,
	};
};

export const messageBlockStyle = props => {
	return {
		margin: "0",
		maxWidth: props.width,
		height: props.height,
	};
};